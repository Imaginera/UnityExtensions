<Window x:Class="Imaginera.UnityExtensions.Example.Views.DashboardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DashboardView" Height="100" Width="500">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBox Grid.Column="0" Text="{Binding Text, UpdateSourceTrigger=PropertyChanged, Delay=500}" />
        <TextBox Grid.Column="1" Text="{Binding Text, UpdateSourceTrigger=PropertyChanged, Delay=500}" />
    </Grid>
</Window>
